{
  "name": "VC-ME",
  "version": "0.0.1",
  "description": "Videocall application using webRTC",
  "main": "app.js",
  "dependencies": {
    "express": "^4.17.1",
    "serve-favicon": "^2.5.0",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  },
  "scripts": {
    "watch": "nodemon --watch \"--exec \"node app.js\"",
    "test": "echo \"Error: no test specified\" && exit 1",
  }
}
